# Alemar11 Tap

## How do I install these formulae?

`brew install alemar11/tap/mangapy`

Or `brew tap alemar11/tap` and then `brew install mangapy`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "alemar11/tap"
brew "mangapy"
```

## Formulae

- `mangapy`: Manga downloader supporting FanFox and MangaDex.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
